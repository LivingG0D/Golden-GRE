# Real per-host tunnel configs live in /etc/golden-gre on each server — never here.
*.conf
!sysctl/*.conf
!**/*.conf.example

# Secrets / credentials — never commit.
.env
*.key
*.pem
*.pub
id_rsa*
secrets*
known_hosts

# Editor / OS cruft
*.swp
*~
.DS_Store
Thumbs.db
